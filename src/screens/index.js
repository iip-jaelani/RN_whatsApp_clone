const {default: Call} = require('./Call');
const {default: Camera} = require('./Camera');
const {default: Home} = require('./Home');
const {default: Status} = require('./Status');

export const TopNavScreen = {
  Camera,
  Home,
  Status,
  Call,
};
